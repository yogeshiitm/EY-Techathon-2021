from .models import MedicalModel

class Eligibility_Score:
    def __init__(self):
        pass
    
    def age(self):
        pass
    
    def 

    def ranking(self):
        users = MedicalModel.objects.all()
        
        for user in users:
            S = user.covid + user. + user. +user.covid + user. + user.+user.covid + user. + user.+user.covid + user. + user.+user.covid + user. + user.+user.covid + user.
            S.append(S) # function
        
        for i in range:
            for user in users.objects.filter(batch ==i):
                R.append() score
                sort
        return



        